export const githubConfig = {
  username: ``,
  repoList: [] as string[],
  branch: `main`,
  accessTokenList: [] as string[],
}

export const giteeConfig = {
  username: ``,
  repoList: [] as string[],
  branch: `main`,
  accessTokenList: [] as string[],
}
